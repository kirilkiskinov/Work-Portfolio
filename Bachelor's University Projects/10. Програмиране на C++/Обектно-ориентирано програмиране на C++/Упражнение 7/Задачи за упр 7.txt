Задачи за упражнение 5 по ООП на С++

1) Да си припомним пример от лекция 6 за обобщен клас на опашка, но тук е даден под друго име, Опашка_1.cpp

2) Ще направим подобрения на програмата от точка 1. за да получим Опашка_2.cpp (но ще имаме и междинни версии между Опашка_1 и Опашка_2) а именно:

а) Ще сложим втори параметър size в списъка на шаблона, който да задава дължината на опашката. 
(т.е глобалната константа SIZE няма да я ползваме повече.) При това нека size да има и стойност по подразбиране стойност 4. 
Обобщеният тип на опашката също ще има стойност по подразбиране - int.
За всички, които са забравили напомням, че стойностите по подразбиране по подразбиране се задават само веднъж,
 при първото срещане на списъка на шаблона.

б) Да създадем в main() обект-опашка, която да използва параметрите по подразбиране. Да вмъкнем 3 стойности в опашката и след това да извлечем 
2. После да се опитаме да вмъкнем още две стойности. Какво наблюдаваме - въпреки, че има свободни елементи в началото на опашката получаваме 
съобщение, че опашката е пълна!

в) Ще направим опашката кръгова. Когато е достигнат последният елемент на масива: 
  - при запис:  rloc да се подготвя да четем от началото на масива
  - при четене: sloc да се подготвя да пишем в началото на масива
  С други думи правим опашката кръгова.
 Ще въведем също така още един елемент закрит count в опашката - за броя на елементите в опашката. 
Чрез count най-лесно се проверява кога опашката е празна или запълнена! Каква ще е проверката? 
Естествено, ще добавим код в qget() и  в qput да поддържаме актуална стойността на count!
Повтаряме стъпките от б) за да се убедим, че можем да добавим двете стойности от предишния сценарий.

г) Вместо да извикваме qput()  с литерали ще използваме шаблон на нова член-функция, да я наречем inputData(), 
която да въвежда стойността от клавиатура. Новата функция ще бъде аргумент за qput(). 
Нека първата инструкция в inputData() да бъде cin.sync();
за изчистване на входния буфер. 
Изпробвайте за опашка с тип double, след това с тип string.
Какво се случва когато се опитаме да четем от празна опашка от тип string?
Как може да решим възникналия бъг?
Коментирайте реда с празната опашка и после въведете текст първо на латиница "da proverim", после въведете на български "да видим". Какви проблеми наблюдаваме.
Да направим специализация на шаблона за типа string.
ВАЖНО! Вместо да въвеждаме cin >> buf; в специализацията ще използваме нова функция getline() по следния начин getline(cin, buf); Така даваме възможност във въведения тескт да има табулации и интервали!

ВНИМАНИЕ! Ако искаме да въвеждаме и символи на кирилица, трябва да ползваме и функцията encode() от заглавния файл devC++_encoding.h

Тук възниква нов проблем, при специализацията трябва и двата параметъра да са константи. С други думи ще ползваме специализацията, само ако създаваме опашка с размер, равен на посочения в специализацията -  което отново ни лишава от възможност да имаме адаптивен размер на опашката.

Затова се връщаме към шаблон с един параметър за типа, а размерът става закрита член-данна, която да е параметър на конструктора. Параметърът ще има и стойност по-подразбиране, за да може да имаме и конструктор без параметри.

След като в main() е обявен обект-опашка да се използва следният код за да добавяме и четем от опашката:

  char choice;
  do {
    cout << "Въведете 'к' за Край, 'п' за постави, 'и' за извлечи: ";
    cin >> choice;
    choice = encode(choice);// заради различните кодировки
    if(choice == 'п' || choice == 'П'){
    	myq.qput(myq.inputData());
    }
    if(choice == 'и'|| choice == 'И'){
    	cout << myq.qget() << endl;
    }
  } while (choice != 'К'  && choice != 'к');

3. Следващо усложнение на нашата опашка е тя да може да добавя и чете обекти от класа addressInBg, който е даден във файла addressInBg.h.  Ще се наложи да направим специализации на шаблонните функции inputData() и printData() за да можем да въвеждаме и извеждаме обекти от новия клас. Нека новият вариант  на опашката бъде Опашка_3.cpp.

Добавете към шаблоните и следния:
 
template <typename QType> 
void queue<QType>::printData(QType buf){
	cout << "Извлечено: " << buf << "\n";
}

и заменете в main() реда cout << myq.qget() << endl;
с реда  myq.printData(myq.qget());

4. Да включим и обработката на изключения. Създайте клас MyException със закрит елемент от тип string,  който да съдържа текста на изключението с подходящ конструктор и гетър. Заменете в qget() и в qput() съобщенията при празна и пълна опашка с хвърляне на изключение от клас MyException като използвате старите съобщения когато създавате обекта на класа MyException. Нека новият вариант  на опашката бъде Опашка_4.cpp.